asm int syscall_len_test() {
    syntax arm64_macho
    mov x0, #1
    adr x1, msg
    mov x2, len(msg)
    mov x16, #4
    svc #0x80
    ret
    msg: asciz "hello syscall\n"
}

int main() {
    return syscall_len_test();
}