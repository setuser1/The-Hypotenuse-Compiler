// Test: rsplit behavior with @ in function name
// Edge case: function name contains @ (shouldn't happen but let's test)
using func from "lib"

int main() {
    // This would use rsplit to get func, lib
    func@lib("test");
    return 0;
}
