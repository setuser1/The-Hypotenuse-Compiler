using "../plstd/string"
expose string

int main() {
    string a = "hello";
    string b = "world";

    // This should work because the whole library is exposed
    int result = strcmp(a, b);
    return result;
}
