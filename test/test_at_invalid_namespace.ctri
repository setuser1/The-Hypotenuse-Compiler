// Test: @ namespace handling - invalid namespace (E804)
// This should produce an error: Invalid alias
using printd from "plstd"

int main() {
    printd@nonexistent("Should fail\n");
    return 0;
}
