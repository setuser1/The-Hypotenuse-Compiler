asm void print(string text) {
    syntax arm64_macho
    .section __TEXT,__text
    mov x1, text
    mov x2, len(text)
    mov x0, #1
    bl _write
}

int main() {
    print("hello\n");
    return 0;
}
