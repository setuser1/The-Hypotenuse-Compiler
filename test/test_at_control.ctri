// Test: @ namespace handling - no @ syntax (control test)
// This should also work without @ syntax
using printf from "stdio"
expose stdio

int main() {
    printf("Hello without @\n");
    return 0;
}
