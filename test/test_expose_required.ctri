expose string
using "../plstd/string"

int main() {
    string a = "hello";
    string b = "world";
    
    // This should work because string is exposed
    int result = strcmp(a, b);
    return result;
}
