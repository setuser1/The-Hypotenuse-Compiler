{
  "name": "ctriangle",
  "displayName": "C△ (C Triangle)",
  "description": "Language support for C△ (C Triangle) - a C superset with namespaces, dynamic arrays, and first-class strings",
  "version": "1.0.0",
  "publisher": "ctriangle",
  "engines": {
    "vscode": "^1.70.0"
  },
  "main": "./out/extension.js",
  "activationEvents": [
    "onLanguage:ctriangle"
  ],
  "scripts": {
    "compile": "tsc -p ./tsconfig.json",
    "package": "vsce package"
  },
  "categories": [
    "Programming Languages"
  ],
  "contributes": {
    "languages": [
      {
        "id": "ctriangle",
        "aliases": ["C△", "C Triangle"],
        "extensions": [".ctri", ".plib"],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "ctriangle",
        "scopeName": "source.ctriangle",
        "path": "./syntaxes/ctriangle.tmLanguage.json"
      }
    ],
    "iconThemes": [
      {
        "id": "ctriangle-icons",
        "label": "C△ Icons",
        "path": "./icons/ctriangle-icon-theme.json"
      }
    ],
    "tasks": {
      "version": "2.0.0",
      "tasks": [
        {
          "type": "shell",
          "label": "Compile C△ File",
          "command": "hypotenuse",
          "args": ["-c", "${file}"],
          "problemMatcher": [],
          "group": "build",
          "detail": "Compile the current C△ file to executable"
        },
        {
          "type": "shell",
          "label": "Run C△ File",
          "command": "${fileDirname}/${fileBasenameNoExtension}",
          "problemMatcher": [],
          "group": "test",
          "dependsOn": ["Compile C△ File"],
          "detail": "Run the compiled executable"
        }
      ]
    }
  },
  "icon": "assets/logo.png"
}
