#include <stdio.h>
using printd from <plstd>

int get_string_length(string s) {
    return len(s);
}

int main() {
    string test = "hello";
    printd("Length: %d\n", get_string_length(test));
    return 0;
}